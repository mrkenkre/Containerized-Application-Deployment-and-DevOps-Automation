apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prometheus-custom.fullname" . }}-test-connection"
  labels:
    {{- include "prometheus-custom.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prometheus-custom.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
